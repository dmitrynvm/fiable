# Fiable model compression pipeline
typer>=0.12.0
rich>=13.7.0
huggingface_hub>=0.23.0
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.0.0

# Evaluation (MMLU, GSM8K, WikiText, GGUF backends)
lm-eval>=0.4.0
datasets>=2.14.0
evaluate>=0.4.0
transformers>=4.40.0
accelerate>=0.26.0
llama-cpp-python>=0.2.0
nvidia-ml-py>=12.0.0

# GGUF conversion (convert_hf_to_gguf.py). numpy>=2 is required so
# transformers can import AutoTokenizer; llama.cpp pins numpy~=1.26
# which breaks SciPy/scikit-learn and tokenizer export.
numpy>=2.0.0,<2.8.0
sentencepiece>=0.1.98,<0.3.0
protobuf>=4.21.0,<5.0.0
